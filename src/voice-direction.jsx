import React from 'react';
import {Field} from './ui';
import {VIDS_VOICES,VIDS_AVATARS,DELIVERY,VOCALIZATIONS} from '../server/vids-voices.mjs';
export default function VoiceDirection(){return <><div className="inspector-section-title">声・人物・演技</div><Field label="ナレーションの声"><select name="voiceName" defaultValue="Nyla">{VIDS_VOICES.map(([name,description])=><option key={name} value={name}>{name} · {description}</option>)}</select></Field><Field label="人物アバター"><select name="avatarId" defaultValue=""><option value="">声のみ（人物なし）</option>{['リアル','3D アニメ','2D アニメ'].map(category=><optgroup key={category} label={category}>{VIDS_AVATARS.filter(a=>a.category===category).map(a=><option value={a.id} key={a.id}>{a.name}</option>)}</optgroup>)}</select><small>人物を使う場合は、そのアバター固有の声で生成します。</small></Field><div className="field-row"><Field label="読み方"><select name="delivery">{DELIVERY.map(([id,label])=><option key={id} value={id}>{label}</option>)}</select></Field><Field label="台本の終わりに入れる声"><select name="vocalization">{VOCALIZATIONS.map(([id,label])=><option key={id} value={id}>{label}</option>)}</select></Field></div><Field label="細かい演技指示"><textarea name="direction" rows={3} maxLength={2000} placeholder="耳元でそっと秘密を伝える。最後は楽しそうに短く笑う…"/></Field><small>Vids用の人物・演技指示と音声タグ付き台本を作ります。Vidsでプレビューして生成します。</small></>;}
